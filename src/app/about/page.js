import AboutContent from './about-content';

export const metadata = {
  title: 'About',
  description:
    'About Vimal Desai — ten years shipping full-stack products across ERP, PropTech, travel, and platform work.',
};

export default function AboutPage() {
  return <AboutContent />;
}
