import ContactContent from './contact-content';

export const metadata = {
  title: 'Contact',
  description:
    'Get in touch with Vimal Desai for full-stack engineering roles, product builds, and select consulting.',
};

export default function ContactPage() {
  return <ContactContent />;
}
